import { createTheme } from "@mui/material"

const themePersonalizado = createTheme({
    palette: {
        primary: {
            main: '#c4460cff',
        },
    },
})

export default themePersonalizado